pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
// repositories will be defined at project level

rootProject.name = "Errand"
include(":app")
